"""The comparison: every scenario this project can run, side by side, in one table.

Each individual run already reports its own verdict. What this adds is the thing a reader actually
needs — the ability to see the *same* burst, against the *same* store, produce a perfect ledger in
one row and an oversold drop three rows down, with the single differing column visible between them.
"""
