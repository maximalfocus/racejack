"""The secure application: every check-and-act pair is one atomic operation.

There is no code in this package that reads a condition, decides on it, and then acts on the
decision as a separate step. That absence is the whole point.
"""
